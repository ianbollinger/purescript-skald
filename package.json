{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp browserify --optimise --to dist/app.js"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.2",
    "purescript-psa": "^0.3.9"
  }
}
