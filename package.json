{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "jshint src && jscs src && pulp browserify --optimise --to dist/skald.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "jscs": "^3.0.7",
    "jshint": "^2.9.2",
    "pulp": "^9.0.1",
    "purescript": "^0.9.2",
    "purescript-psa": "^0.3.9"
  }
}
